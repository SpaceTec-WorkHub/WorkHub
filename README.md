
  # WorkHub MTY UI Prototype

  This is a code bundle for WorkHub MTY UI Prototype. The original project is available at https://www.figma.com/design/rsAfLxX7PVGUjw1r9L685E/WorkHub-MTY-UI-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  